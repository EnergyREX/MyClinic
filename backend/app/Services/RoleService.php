<?php 

namespace App\Services;

class RoleService {

  function createRole() {
    // Debe de obtener los permisos existentes
    // Después de obtener los permisos detectará si el permiso es "true".
    // Si es "true" un permiso, dicho permiso es asignado (Pivot table roles_permissions)
  }
}